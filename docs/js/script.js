document.addEventListener('DOMContentLoaded', () => {
    // --- Dropdown Menu ---
    // Bootstrap's JS handles dropdown functionality automatically via data-bs-toggle attributes.
    // No custom JS needed for basic dropdowns.

    // --- Carousel Functionality ---
    // Bootstrap's JS handles carousel functionality automatically via data-bs-ride/data-bs-slide attributes.
    // No custom JS needed for basic carousel operation.

    // Any other custom JavaScript specific to your page can go here.
    console.log("Club Landa page loaded with Bootstrap 5.");

});

